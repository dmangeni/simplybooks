import * as lib from '../utils/createreducer'
import * as types from '../actions/types'
//import Firebase from 'firebase'

//const simply = new Firebase('https://simplybooks-676c4.firebaseio.com');

/*export function fetch_user_detail(){
  return(dispatch,getState) => {
    simply.on('value', snapshot => {
      dispatch({
        type: FETCH_USER_LOGIN,
        snapshot
      });
    });
  }
}

export function create_user(){

}
export function delete_user(key){

}*/
